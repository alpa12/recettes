name: Import Recipe from YouTube

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'recettes/url_imports_youtube/**/*.yaml'
      - 'livrec/**'

jobs:
  import-recipe-youtube:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.5.2'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libxml2-dev libssl-dev yt-dlp python3-pip

      - name: Install Python transcript dependency
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install youtube-transcript-api

      - name: Restore R packages with renv
        uses: r-lib/actions/setup-renv@v2

      - name: Install livrec package and dependencies
        run: |
          Rscript -e 'source("renv/activate.R"); deps <- unique(na.omit(renv::dependencies("livrec")$Package)); deps <- setdiff(deps, "livrec"); if (length(deps) > 0) renv::install(deps, prompt = FALSE, repos = "https://cloud.r-project.org"); renv::install("./livrec", prompt = FALSE)'

      - name: Download CNF dataset (Health Canada)
        run: |
          mkdir -p data/nutrition/cnf
          curl -fL "https://www.canada.ca/content/dam/hc-sc/migration/hc-sc/fn-an/alt_formats/zip/nutrition/fiche-nutri-data/cnf-fcen-csv.zip" -o /tmp/cnf-fcen-csv.zip
          unzip -o /tmp/cnf-fcen-csv.zip -d data/nutrition/cnf

      - name: Build CNF foods mapping file
        run: |
          Rscript -e 'source("renv/activate.R"); livrec::gha_build_cnf_foods()'

      - name: Find pending YouTube URL file
        id: find-youtube
        run: |
          URL_FILE=$(find recettes/url_imports_youtube -name "*.yaml" -type f 2>/dev/null | sort | head -n 1)

          if [ -z "$URL_FILE" ]; then
            echo "Aucune demande YouTube à traiter."
            echo "run=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "run=true" >> "$GITHUB_OUTPUT"
          echo "url_file=$URL_FILE" >> "$GITHUB_OUTPUT"
          echo "Fichier YouTube trouvé: $URL_FILE"

      - name: Import recipe from YouTube
        id: import-youtube
        if: steps.find-youtube.outputs.run == 'true'
        continue-on-error: true
        env:
          RECIPE_URL_FILE: ${{ steps.find-youtube.outputs.url_file }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          Rscript -e 'source("renv/activate.R"); livrec::gha_import_recipe_from_youtube()'

      - name: Commit generated recipe files
        if: steps.find-youtube.outputs.run == 'true' && steps.import-youtube.outcome == 'success'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          git add recettes/**/*.yaml recettes/**/*.qmd
          git rm ${{ steps.find-youtube.outputs.url_file }}

          if git diff --staged --quiet; then
            echo "Aucun changement à commiter"
          else
            git commit -m "🤖 Import automatique de recette depuis YouTube"
            git push
          fi

      - name: Comment on PR
        if: steps.find-youtube.outputs.run == 'true' && steps.import-youtube.outcome == 'success'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ **Import YouTube terminé!**\n\nLa transcription a été analysée et la recette a été générée.'
            })

      - name: Comment on PR (manual handling required)
        if: steps.find-youtube.outputs.run == 'true' && steps.import-youtube.outcome != 'success'
        uses: actions/github-script@v7
        with:
          script: |
            const branch = context.payload.pull_request.head.ref;
            const urlFile = '${{ steps.find-youtube.outputs.url_file }}';
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `⚠️ **Import YouTube automatique échoué**

La vidéo semble bloquer l'accès automatisé à la transcription. La demande URL est conservée pour traitement manuel.

Commande à copier-coller dans ton terminal:

\`\`\`bash
cd /Users/alexandreparent/Documents/r-projects/recettes
git fetch origin
git checkout ${branch}
git pull --ff-only origin ${branch}

export RECIPE_URL_FILE="${urlFile}"
export GEMINI_API_KEY="$(grep '^GEMINI_API_KEY=' .Renviron | sed 's/^GEMINI_API_KEY=//')"

Rscript -e 'source("renv/activate.R"); renv::install("./livrec", prompt = FALSE); livrec::gha_import_recipe_from_youtube()'

git add recettes/**/*.yaml recettes/**/*.qmd
git rm "${urlFile}"
git commit -m "🤖 Import manuel recette depuis YouTube"
git push origin ${branch}
\`\`\``
            })
